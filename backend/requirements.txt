# SentinelIQ Backend — Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
alembic>=1.13.0
pgvector>=0.3.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-jose[cryptography]>=3.3.0
httpx>=0.27.0

# LLM & Graph
langchain-core>=0.3.0
langchain-groq>=0.2.0
langgraph>=0.2.0
langgraph-checkpoint-redis>=0.1.0

# Embeddings — Voyage AI ONLY (no OpenAI fallback)
voyageai>=0.2.0

# Reranker — Cohere (optional second stage after RRF)
cohere>=5.9.0

# Knowledge Graph — Neo4j
neo4j>=5.24.0

# Checkpointer — Redis
redis>=5.1.0

# Log Parsers
python-evtx>=0.7.0

# PDF Reports
reportlab>=4.2.0

# Observability
langfuse>=2.51.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
aiosqlite>=0.20.0

# Eval (optional)
# ragas>=0.2.0
# datasets>=3.0.0
# sentence-transformers>=3.0.0
